import { MessageNotifyInterceptor } from './message-notify.interceptor.js';

describe('MessageNotifyInterceptor', () => {
  it('should be defined', () => {
    expect(new MessageNotifyInterceptor()).toBeDefined();
  });
});
