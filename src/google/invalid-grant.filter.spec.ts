import { InvalidGrantFilter } from './invalid-grant.filter.js';

describe('InvalidGrantFilter', () => {
  it('should be defined', () => {
    expect(new InvalidGrantFilter()).toBeDefined();
  });
});
