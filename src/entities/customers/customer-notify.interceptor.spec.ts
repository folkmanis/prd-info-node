import { CustomerNotifyInterceptor } from './customer-notify.interceptor.js';

describe('CustomerNotifyInterceptor', () => {
  it('should be defined', () => {
    expect(new CustomerNotifyInterceptor()).toBeDefined();
  });
});
