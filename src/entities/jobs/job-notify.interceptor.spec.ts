import { JobNotifyInterceptor } from './job-notify.interceptor';

describe('JobNotifyInterceptor', () => {
  it('should be defined', () => {
    expect(new JobNotifyInterceptor()).toBeDefined();
  });
});
