import { UserUpdateNotifyInterceptor } from './user-update-notify.interceptor.js';

describe('UserUpdateNotifyInterceptor', () => {
  it('should be defined', () => {
    expect(new UserUpdateNotifyInterceptor()).toBeDefined();
  });
});
