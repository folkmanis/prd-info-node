import { RequestParametersPipe } from './request-parameters.pipe.js';

describe('RequestParametersPipe', () => {
  it('should be defined', () => {
    expect(new RequestParametersPipe()).toBeDefined();
  });
});
