import { QueryStartLimitPipe } from './query-start-limit.pipe';

describe('QueryStartLimitPipe', () => {
  it('should be defined', () => {
    expect(new QueryStartLimitPipe()).toBeDefined();
  });
});
